"""Drop-in folder for one-off commands (slash or prefix).

Any `.py` file placed here is auto-discovered and loaded by bot.py at
startup — no registration needed. Files whose name starts with `_` are
skipped (see `_template.py` for the pattern to copy).
"""
